set -g focus-events on
set -g monitor-activity on
setw -g aggressive-resize on
# set -g extended-keys-format csi-u

# set -g default-terminal "xterm-256color" # tmux-256color
# set -s terminal-overrides ",xterm-256color:RGB"

# bind kill-window

bind Q confirm-before -p "kill-session #S? (y/n)" "kill-session"
bind N command-prompt -p "Enter session name: " "new-session -A -s '%%'"

setw -g allow-rename off

# ―――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

set -g @plugin 'junegunn/tmux-fzf-url'
set -g @plugin 'tmux-plugins/tmux-fpp'

# ―――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――

# show session name (#S) and, if set, show the server-label in brackets right after it
# set -g status-left "#[fg=green] #S #(sh -c 'v=$(tmux show -gqv @server_label 2>/dev/null); if [ -n \"\$v\" ]; then printf \" [%s]\" \"\$v\"; fi')"

# key binding: prefix + L  -> prompt to set/edit label (leave empty to clear)
# bind-key L command-prompt -p "Server label (leave empty to clear):" "run-shell 'if [ -z \"%%\" ]; then tmux set-option -gu @server_label; else tmux set-option -g @server_label \"%%\"; fi; tmux refresh-client -S'"

# key binding: prefix + U  -> clear label immediately
# bind-key U run-shell "tmux set-option -gu @server_label; tmux refresh-client -S" \; display-message "Server label cleared"
# ―――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――
# set -g @fzf-url-bind 'u'
# https://github.com/wfxr/tmux-fzf-url
# set -g @plugin 'jabirali/tmux-tilish'

# setw -g xterm-keys on

# bind b list-buffers
# bind p paste-buffer -p
# bind p paste-buffer
# bind P choose-buffer
# bind b choose-buffer -Z
# bind ??? choose-tree
# bind C-l send-keys 'C-l'
#     "Join Pane"                          j "choose-window 'join-pane -h -s \"%%\"'" \

# set -g repeat-time 300 # -sg
# set-option -g remain-on-exit on
# set -g allow-passthrough on
# set -g window-status-style "bg=default"
# set -g window-status-last-style "bg=default"
# set -g window-status-current-style 'bg=default'
# set -g status-bg 'default'

# bind-key -N "Open Navi (cheat sheets)" -T prefix C-g split-window \
#   "$SHELL --login -i -c 'navi --print | head -n 1 | tmux load-buffer -b tmp - ; tmux paste-buffer -p -t {last} -b tmp -d'"
# bind C-r new-window -n 'tmux.conf' "sh -c '\${EDITOR:-vim} ~/.tmux.conf && tmux source ~/.tmux.conf && tmux display \"Config reloaded\"'"
# setw -g automatic-rename off

# -------------------------------
# -------------------------------

bind -T copy-mode-vi C-v send-keys -X rectangle-toggle
set -g allow-passthrough on

# F12 nested session toggle
bind -T root F12 \
  set prefix None \;\
  set key-table off \;\
  set status-style "fg=default,bg=#2D4F67" \;\
  if -F '#{pane_in_mode}' 'send-keys -X cancel' \;\
  refresh-client -S

bind -T off F12 \
  set -u prefix \;\
  set -u key-table \;\
  set -u status-style \;\
  refresh-client -S

# Show [OFF] indicator when keys are off
set -g status-right '#([ $(tmux show-option -qv key-table) = "off" ] && echo " [OFF] ")'

# Window swapping
bind -r Left swap-window -t -1 \; select-window -t -1
bind -r Right swap-window -t +1 \; select-window -t +1

# Agent notifications
setw -g monitor-bell on
set -g bell-action other # only monitor bells in windows not currently open
setw -g window-status-bell-style 'fg=#787487 underscore'
