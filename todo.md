> toggle auto completion

cmdheight could be made zero (best would be have something like noice to have
cmdline at the center instead)

tabline should only show when having more than one buffer

use sesh last

git autocompletion doesn't work with alias `g` 

tmux copy doesn't work in tmux popup (in remote machine)

nvim being used as git difftool, whne invoked it list the diff on the files one by one. how should i close the entire process in between?

how to easily toggle the openrouter models inside codecompanion
